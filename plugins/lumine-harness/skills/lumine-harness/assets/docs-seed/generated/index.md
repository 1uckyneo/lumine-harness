# Generated

`docs/generated/**` 是静态扫描 + 模型复核后的导航索引，不是事实源。

可刷新 target：

- `workspace-index`
- `repo-doc-index`
- `api-map`
- `db-schema`
- `frontend-routes`
- `frontend-components`

刷新后必须做模型复核，写回 `Review status`、`Reviewed at`、`Reviewer`、`Review scope` 和 `Known gaps`。
