// @generated by pnpm runtime:build from TypeScript source; do not edit directly.
// Source: skills/lumine-harness/src/harness/core/root-resolver.ts
import { existsSync, readFileSync } from "node:fs";
import path from "node:path";

//#region skills/lumine-harness/src/harness/core/root-resolver.ts
const HARNESS_ROOT_MARKER = path.join(".harness", "root.json");
function isHarnessMarker(file) {
	try {
		const value = JSON.parse(readFileSync(file, "utf8"));
		return value?.kind === "harness-root" && Number(value?.schemaVersion) >= 1;
	} catch {
		return false;
	}
}
function findHarnessRoot(start = process.cwd()) {
	let current = path.resolve(start);
	while (true) {
		const marker = path.join(current, HARNESS_ROOT_MARKER);
		if (existsSync(marker) && isHarnessMarker(marker)) return current;
		const parent = path.dirname(current);
		if (parent === current) return null;
		current = parent;
	}
}
function resolveHarnessRoot(input = {}) {
	const starts = [
		input.cwd,
		...input.workspaceRoots ?? [],
		process.cwd()
	].filter(Boolean);
	for (const start of starts) {
		const root = findHarnessRoot(start);
		if (root) return root;
	}
	return null;
}
function requireHarnessRoot(input = {}) {
	const root = resolveHarnessRoot(input);
	if (root) return root;
	const error = Object.assign(new Error("Harness root not found. Start from the workspace containing .harness/root.json."), { code: "HARNESS_ROOT_NOT_FOUND" });
	throw error;
}
function isStartedAtHarnessRoot(root, cwd = process.cwd()) {
	return path.resolve(root) === path.resolve(cwd);
}

//#endregion
export { HARNESS_ROOT_MARKER, findHarnessRoot, isStartedAtHarnessRoot, requireHarnessRoot, resolveHarnessRoot };