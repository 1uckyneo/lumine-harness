// @generated by pnpm runtime:build from TypeScript source; do not edit directly.
// Source: skills/lumine-harness/src/harness/core/verification.ts
import { appendFileSync, existsSync, mkdirSync, readFileSync, readdirSync, writeFileSync } from "node:fs";
import path from "node:path";
import { createHash, randomUUID } from "node:crypto";

//#region skills/lumine-harness/src/harness/core/verification.ts
const ADAPTER_CAPABILITIES = [
	"project_instructions",
	"session_context",
	"skill_discovery",
	"skill_read",
	"pre_mutation_gate",
	"stop_gate",
	"automatic_continuation",
	"work_status_matrix",
	"session_isolation"
];
const TOOL_EVENT_PRODUCTS = new Set([
	"qoder",
	"zcode",
	"codebuddy",
	"deepseek-harness"
]);
function safe(value) {
	return String(value ?? "").replace(/[^a-z0-9_.-]+/gi, "_").slice(0, 160);
}
function verificationRoot(root) {
	return path.join(root, ".harness", "runtime", "probes");
}
function activeProbeFile(root, product) {
	return path.join(verificationRoot(root), "active", `${safe(product)}.json`);
}
function sha256(value) {
	return createHash("sha256").update(String(value)).digest("hex");
}
function readChallenge(dir) {
	const file = path.join(dir, "probe.json");
	if (!existsSync(file)) return null;
	try {
		return JSON.parse(readFileSync(file, "utf8"));
	} catch {
		return null;
	}
}
function beginVerificationRun(root, product, options = {}) {
	const hostVersion = String(options.hostVersion ?? "unknown").trim() || "unknown";
	const hostVersionSource = options.hostVersion ? options.hostVersionSource ?? "user_reported" : "unknown";
	const runId = safe(options.verificationRunId ?? `${product}--${randomUUID()}`);
	const dir = path.join(verificationRoot(root), runId);
	if (existsSync(path.join(dir, "events.jsonl")) || existsSync(path.join(dir, "probe.json"))) {
		throw new Error(`Verification run already exists: ${runId}`);
	}
	mkdirSync(dir, { recursive: true });
	const issuedAt = new Date();
	const challenge = {
		schemaVersion: 1,
		verificationRunId: runId,
		product,
		hostVersion,
		hostVersionSource,
		rootHash: sha256(path.resolve(root)),
		nonce: randomUUID(),
		issuedAt: issuedAt.toISOString(),
		expiresAt: new Date(issuedAt.getTime() + Number(options.maxAgeMs ?? 24 * 60 * 60 * 1e3)).toISOString()
	};
	writeFileSync(path.join(dir, "probe.json"), `${JSON.stringify(challenge, null, 2)}\n`, "utf8");
	const activeFile = activeProbeFile(root, product);
	mkdirSync(path.dirname(activeFile), { recursive: true });
	writeFileSync(activeFile, `${JSON.stringify({
		product,
		verificationRunId: runId,
		expiresAt: challenge.expiresAt
	}, null, 2)}\n`, "utf8");
	return {
		product,
		status: "challenge_issued",
		verificationRunId: runId,
		hostVersion,
		hostVersionSource,
		message: "请从当前 Harness 根目录开启一个新的真实 Agent 会话；Adapter 会自动发现这次安全探针。"
	};
}
function activeVerificationRunId(root, product) {
	const file = activeProbeFile(root, product);
	if (!existsSync(file)) return null;
	try {
		const active = JSON.parse(readFileSync(file, "utf8"));
		if (active.product !== product || !active.expiresAt || !active.verificationRunId || Date.now() > Date.parse(active.expiresAt)) return null;
		return safe(active.verificationRunId);
	} catch {
		return null;
	}
}
/**
* Reuse the still-active probe for a product, or create a new one when no
* usable probe exists. This is intentionally limited to Harness runtime state;
* it never writes business source files.
*/
function ensureVerificationRun(root, product, options = {}) {
	const activeRunId = activeVerificationRunId(root, product);
	if (!activeRunId) return beginVerificationRun(root, product, options);
	const challenge = readChallenge(path.join(verificationRoot(root), activeRunId));
	if (!challenge) return beginVerificationRun(root, product, options);
	return {
		product,
		status: "challenge_issued",
		verificationRunId: challenge.verificationRunId,
		hostVersion: challenge.hostVersion,
		hostVersionSource: challenge.hostVersionSource,
		message: "已有仍在有效期内的安全探针，本次将继续复用。",
		reused: true
	};
}
function verificationRunId(input, raw = {}, root = null) {
	const requested = raw.verification_run_id ?? raw.verificationRunId;
	if (requested) return safe(requested);
	return root ? activeVerificationRunId(root, input.product) : null;
}
function appendVerificationEvent(root, input, details = {}) {
	if (!input?.product || !input?.sessionId || input.sessionId === "unknown") throw new Error("Runtime evidence requires explicit product and sessionId.");
	const runId = verificationRunId(input, details.raw ?? {}, root);
	if (!runId) return null;
	const dir = path.join(verificationRoot(root), runId);
	const challenge = readChallenge(dir);
	if (!challenge || challenge.product !== input.product || Date.now() > Date.parse(challenge.expiresAt)) return null;
	mkdirSync(dir, { recursive: true });
	const relativeCwd = path.relative(root, path.resolve(input.cwd)) || ".";
	const record = {
		schemaVersion: 1,
		verificationRunId: runId,
		product: input.product,
		event: input.event,
		sessionIdHash: sha256(input.sessionId),
		cwd: relativeCwd.split(path.sep).join("/"),
		at: new Date().toISOString(),
		challengeHash: challenge ? sha256(JSON.stringify(challenge)) : null,
		hostVersion: challenge?.hostVersion ?? null,
		hostVersionSource: challenge?.hostVersionSource ?? "unknown",
		observations: Array.isArray(details.observations) ? [...new Set(details.observations.filter((item) => typeof item === "string" && ADAPTER_CAPABILITIES.includes(item)))] : undefined,
		skill: details.skill ? {
			name: details.skill.name,
			source: details.skill.relativeSource,
			hash: details.skill.hash
		} : undefined,
		decision: details.decision ? {
			action: details.decision.action,
			disposition: details.decision.disposition ?? null,
			workStatus: details.decision.workStatus ?? null,
			workStatusRevision: details.decision.workStatusRevision ?? null,
			continuationRequestId: details.decision.continuationRequestId ?? null,
			shouldDeliver: details.decision.shouldDeliver ?? null
		} : undefined
	};
	appendFileSync(path.join(dir, "events.jsonl"), `${JSON.stringify(record)}\n`, "utf8");
	return record;
}
function readEvents(file) {
	return readFileSync(file, "utf8").split(/\r?\n/).filter(Boolean).map((line) => JSON.parse(line));
}
function candidateRuns(root, product) {
	const dir = verificationRoot(root);
	if (!existsSync(dir)) return [];
	return readdirSync(dir, { withFileTypes: true }).filter((entry) => entry.isDirectory()).map((entry) => {
		const file = path.join(dir, entry.name, "events.jsonl");
		try {
			const events = existsSync(file) ? readEvents(file) : [];
			const filtered = events.filter((event) => event.product === product);
			const challenge = readChallenge(path.join(dir, entry.name));
			if (challenge?.product !== product && !filtered.length) return null;
			return {
				runId: entry.name,
				file,
				challenge,
				events: filtered
			};
		} catch {
			return null;
		}
	}).filter((item) => item !== null).sort((left, right) => {
		const rightAt = right.events.at(-1)?.at ?? right.challenge?.issuedAt ?? "";
		const leftAt = left.events.at(-1)?.at ?? left.challenge?.issuedAt ?? "";
		return Date.parse(rightAt) - Date.parse(leftAt);
	});
}
function capabilityDefaults(product) {
	return Object.fromEntries(ADAPTER_CAPABILITIES.map((capability) => {
		let result = "not_tested";
		if (["skill_read", "pre_mutation_gate"].includes(capability) && !TOOL_EVENT_PRODUCTS.has(product)) result = "not_observable";
		if (product === "opencode" && [
			"stop_gate",
			"automatic_continuation",
			"work_status_matrix"
		].includes(capability)) result = "not_applicable";
		const evidenceLevel = "official_declared";
		return [capability, {
			result,
			evidenceLevel,
			observedAt: null,
			evidence: null
		}];
	}));
}
function summarizeCapabilities(product, events, evidence) {
	const capabilities = capabilityDefaults(product);
	const observedAt = events.at(-1)?.at ?? null;
	const observations = new Set(events.flatMap((event) => event.observations ?? []));
	if (events.some((event) => ["session_start", "prompt_submit"].includes(event.event))) observations.add("session_context");
	if (events.some((event) => event.skill?.source?.startsWith(".agents/skills/"))) {
		observations.add("skill_discovery");
		observations.add("skill_read");
	}
	if (events.some((event) => event.event === "stop")) observations.add("stop_gate");
	const statuses = new Set(events.filter((event) => event.event === "stop").map((event) => event.decision?.workStatus).filter((status) => Boolean(status)));
	const allWorkStatuses = [
		"done",
		"continue_autonomously",
		"needs_user_decision",
		"needs_credentials",
		"needs_manual_app_step",
		"blocked_external"
	];
	if (allWorkStatuses.every((status) => statuses.has(status))) {
		observations.add("work_status_matrix");
	}
	const continuationIndex = events.findIndex((event) => event.event === "stop" && event.decision?.disposition === "request_continuation" && event.decision?.continuationRequestId);
	if (continuationIndex >= 0 && events.slice(continuationIndex + 1).some((event) => event.event !== "stop")) observations.add("automatic_continuation");
	const sessions = new Map();
	for (const event of events) {
		const items = sessions.get(event.sessionIdHash) ?? [];
		items.push(event);
		sessions.set(event.sessionIdHash, items);
	}
	if (sessions.size >= 2 && [...sessions.values()].every((items) => items.some((event) => ["session_start", "prompt_submit"].includes(event.event)) && items.some((event) => event.event === "stop" && Number.isInteger(event.decision?.workStatusRevision)))) observations.add("session_isolation");
	for (const capability of observations) {
		if (!capabilities[capability] || capabilities[capability].result === "not_applicable") continue;
		capabilities[capability] = {
			result: "passed",
			evidenceLevel: "runtime_observed",
			observedAt,
			evidence
		};
	}
	return capabilities;
}
function failedEvidence(product, message) {
	return {
		product,
		status: "failed",
		capabilities: capabilityDefaults(product),
		messages: [message]
	};
}
function verifyRuntimeEvidence(root, product, options = {}) {
	const runs = candidateRuns(root, product);
	const activeRunId = activeVerificationRunId(root, product);
	const run = options.verificationRunId ? runs.find((item) => item.runId === safe(options.verificationRunId)) : runs.find((item) => item.runId === activeRunId) ?? runs.find((item) => item.challenge);
	if (!run) return {
		product,
		status: "not_tested",
		capabilities: capabilityDefaults(product),
		messages: ["没有找到有效的安全探针或真实会话事件。"]
	};
	if (!run.challenge) return failedEvidence(product, "这组证据不是由 Lumine Harness 发出的安全探针创建的。");
	const challenge = run.challenge;
	if (challenge.product !== product || challenge.rootHash !== sha256(path.resolve(root))) {
		return failedEvidence(product, "安全探针属于其他 Agent 产品或其他 Harness 根目录。");
	}
	if (Date.now() > Date.parse(challenge.expiresAt)) return failedEvidence(product, "安全探针已经过期，请重新开始连接检查。");
	const challengeHash = sha256(JSON.stringify(challenge));
	if (run.events.some((event) => event.challengeHash !== challengeHash || event.hostVersion !== challenge.hostVersion)) {
		return failedEvidence(product, "真实会话事件与本次安全探针不匹配。");
	}
	if (run.events.some((event) => !event.sessionIdHash)) return failedEvidence(product, "证据中存在无法确认会话身份的事件。");
	if (run.events.some((event) => String(event.cwd).startsWith("..") || path.isAbsolute(event.cwd))) return failedEvidence(product, "证据记录在当前 Harness 根目录之外。");
	const maxAgeMs = Number(options.maxAgeMs ?? 24 * 60 * 60 * 1e3);
	const latestEvent = run.events.at(-1);
	if (!latestEvent) {
		return {
			product,
			status: "not_tested",
			verificationRunId: run.runId,
			hostVersion: challenge.hostVersion,
			hostVersionSource: challenge.hostVersionSource ?? "unknown",
			capabilities: capabilityDefaults(product),
			messages: ["安全探针已经创建，但还没有观察到真实 Agent 会话事件。请继续一次正常交互后再次检查。"]
		};
	}
	if (Date.now() - Date.parse(latestEvent.at) > maxAgeMs) return failedEvidence(product, "真实会话证据已经过期，请重新开始连接检查。");
	const evidence = path.relative(root, run.file).replaceAll(path.sep, "/");
	return {
		product,
		status: "runtime_observed",
		verificationRunId: run.runId,
		hostVersion: challenge.hostVersion,
		hostVersionSource: challenge.hostVersionSource ?? "unknown",
		verifiedAt: latestEvent.at,
		evidence,
		capabilities: summarizeCapabilities(product, run.events, evidence),
		messages: ["安全探针已经观察到真实会话事件；这只证明对应能力被观察到，不代表整个 Agent 已通过完整兼容性认证。"]
	};
}

//#endregion
export { ADAPTER_CAPABILITIES, appendVerificationEvent, beginVerificationRun, ensureVerificationRun, verificationRunId, verifyRuntimeEvidence };