// @generated by pnpm runtime:build from TypeScript source; do not edit directly.
// Source: skills/lumine-harness/src/harness/adapters/cursor/hooks/after-agent-response.ts
import { requireHarnessRoot } from "../../../core/root-resolver.mjs";
import { observeHarnessEvent, writeSessionState } from "../../../core/work-status.mjs";
import { normalizeHookInput, readHookInput } from "../../../core/hook-io.mjs";

//#region skills/lumine-harness/src/harness/adapters/cursor/hooks/after-agent-response.ts
try {
	const raw = await readHookInput();
	const input = normalizeHookInput("cursor", "assistant_response", raw);
	const root = requireHarnessRoot(input);
	observeHarnessEvent(root, input, { eventId: input.eventId });
	writeSessionState(root, input.product, input.sessionId, {
		lastAssistantMessage: typeof raw.text === "string" ? raw.text : "",
		lastAssistantMessageAt: new Date().toISOString()
	});
} catch (error) {
	process.stderr.write(`cursor response hook failed: ${error instanceof Error ? error.message : String(error)}\n`);
	process.exitCode = 1;
}

//#endregion
export {  };